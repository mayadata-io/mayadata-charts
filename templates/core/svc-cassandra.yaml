{{- if or (eq .Values.type "DOP") (eq .Values.type "OEP") }}
apiVersion: v1
kind: Service
metadata:
  labels:
    app: cassandra
  name: cassandra
spec:
  clusterIP: None
  ports:
  - port: 9042
  selector:
    app: cassandra
{{- end }}